# API URL is intentionally left empty here.
# The frontend will use relative path '/api' by default,
# which works correctly whether hosted locally (localhost:5000)
# or on a remote server (indiasilkhouseuae.com).
# VITE_API_URL=https://indiasilkhouseuae.com/api